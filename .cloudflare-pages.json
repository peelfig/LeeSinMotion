{
  "$schema": "https://developers.cloudflare.com/pages/platform/build-configuration",
  "build": {
    "command": "cd website && npm install && npm run docs:build",
    "output_directory": "website/docs/.vuepress/dist"
  }
}
